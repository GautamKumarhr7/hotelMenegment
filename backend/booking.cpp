#include "booking.h"

Booking::Booking(int roomNumber, const std::string &customerName, const std::string &customerEmail)
    : roomNumber(roomNumber), customerName(customerName), customerEmail(customerEmail) {}
